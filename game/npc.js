import Dialogo from "./dialogo.js";

export default class Npc {
  constructor() {
    super({ key: "npc" });
  }

  preload() {}

  create() {
    let nombre; //se le pasara una variable en el construcor

    //Creación de la variable de tipo Dialogo,
    //habrá que pasarle como parámetros rodo lo que necesite Dialogo
  }

  update(time, delta) {}
}
