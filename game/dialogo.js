export default class Dialogo {
  constructor() {
    super({ key: "dialogo" });
  }

  preload() {}

  create() {
    //habria que crear los arrays y todos los mensajes de cada muñeco
  }

  update(time, delta) {}
}
